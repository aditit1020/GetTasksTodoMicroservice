apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: appadi-get-task-api-ingress
spec:
  ingressClassName: azure-application-gateway
  rules:
  - host: get-tasks-api.innervoicebyaditi.online
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: appadi-get-task-api
            port: 
              number: 80
